<div class="contents row">
  <div class="container">
    <h3>投稿する</h3>
    <%= form_with(model: @tweet, local: true) do |f| %>
      <%= f.file_field :image, id:"tweet-image", placeholder: "Image Url" %>
      <%= f.text_field :title, placeholder: "Title" %>
      <%= f.collection_select(:theme_id, Theme.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:prefecture_id, Prefecture.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:mountain_name_id, MountainName.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:climbing_time_id, ClimbingTime.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:stamina_id, Stamina.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:mountain_view_id, MountainView.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>
      <%= f.collection_select(:danger_id, Danger.all, :id, :name, {}, {class:"select-box", id:"tweet-prefecture"}) %>

      <%= f.submit "SEND" %>
    <% end %>
  </div>
</div>